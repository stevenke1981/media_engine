[package]
name = "media_compute_cuda"
version = "0.1.0"
edition = "2021"
description = "CUDA compute backend for the Rust Media Engine"

[dependencies]
media_core = { path = "../media_core" }
media_compute = { path = "../media_compute" }
thiserror.workspace = true
log.workspace = true

[build-dependencies]
